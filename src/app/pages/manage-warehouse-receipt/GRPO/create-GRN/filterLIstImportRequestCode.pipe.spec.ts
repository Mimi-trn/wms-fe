/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterLIstImportRequestCodePipe } from './filterLIstImportRequestCode.pipe';

describe('Pipe: FilterLIstImportRequestCodee', () => {
  it('create an instance', () => {
    let pipe = new FilterLIstImportRequestCodePipe();
    expect(pipe).toBeTruthy();
  });
});
